import React from "react";
import { Box, Text, BorderBox } from "./components";

const App: React.FC = () => {
  return (
    <div style={{ width: "100%" }}>
      <Box mt={2} bg="orange400" />
    </div>
  );
};

export default App;

import { createComponent } from "../helpers/createComponent";
import { Position as IPosition } from "../types/position";

type PositionProps = IPosition;

export const Position = createComponent<PositionProps>();
